language: python
python:
  - "3.5"
  - "3.6"
  - "nightly"
install:
  - pip install -r requirements.txt
script:
  - coverage run --source=lemon setup.py test
  - coveralls
  - pycodestyle --exclude docs/
